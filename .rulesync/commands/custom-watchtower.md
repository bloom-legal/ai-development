<watchtower-auto-updates><description>This project uses Watchtower for automatic Docker container updates. Watchtower is already configured and running - do NOT modify, implement, or touch the Watchtower service itself.</description><requirement>When creating or modifying any docker-compose service that should receive automatic updates, you MUST add the watchtower label to enable auto-updates.</requirement><label>com.centurylinklabs.watchtower.enable=true</label><syntax><example>labels: ["com.centurylinklabs.watchtower.enable=true"]</example><yaml-anchor>x-watchtower-label: &amp;watchtower_label\n  labels: ["com.centurylinklabs.watchtower.enable=true"]</yaml-anchor></syntax><rules><rule>Always add the watchtower label to every new service</rule><rule>Use latest or rolling tags (e.g., image:latest, image:2, image:main) not pinned digests</rule><rule>Never modify the watchtower service configuration</rule><rule>Never create a new watchtower instance</rule></rules><verification>docker inspect container-name --format '{{index .Config.Labels "com.centurylinklabs.watchtower.enable"}}' should return true</verification></watchtower-auto-updates>
